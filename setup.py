from distutils.core import setup

setup(
    name='mvn',
    version='0.0.0dev',
    packages=['mvn'],
    license='BSD',
    install_requires=[
          'numpy','scipy','matplotlib','decorator'
      ]
)
